import styled from "styled-components";

import { CenterDiv } from "../global-css";

const Container = styled(CenterDiv)`
  min-height: 90%;
  width: 50rem;
`;

export const Account = () => {
  return <Container></Container>;
};
