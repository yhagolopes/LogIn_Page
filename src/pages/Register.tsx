import styled from "styled-components";

import { BaseDiv } from "../global-css";

const Container = styled(BaseDiv)`
  width: 50rem;
  min-height: 20rem;
`;

export const Register = () => {
  return <Container></Container>;
};
